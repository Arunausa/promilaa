import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Return & 7-Day Size Exchange Policy | Promilaa Ethnic Wear BD",
  description: "Learn about Promilaa 7-day hassle-free size exchange and defect replacement policy across Bangladesh.",
};

export default function ReturnsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
