# Project-Scoped Rules & Directives for Promilaa

## Deployment Restrictions
- **STRICT DEPLOYMENT RULE:** DO NOT push to Git (`git push`) or deploy to Vercel production (`npx vercel --prod`) until ALL code changes and fixes have been thoroughly tested locally AND explicit confirmation/instruction to deploy is received from the USER.
